[package]
name = "cyberoracle"
version = "0.1.0"
description = "Created with Anchor"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "cyberoracle"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
# 👇 0.30.1 新增：必须添加这个特性才能生成 IDL
idl-build = ["anchor-lang/idl-build"]

[dependencies]
# 👇 升级到 0.30.1，保持 init-if-needed
anchor-lang = { version = "0.30.1", features = ["init-if-needed"] }
